#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '(:lisnail) :silent t)
  )

(defpackage :ros.script.lisnail.3783152360
  (:use :cl))
(in-package :ros.script.lisnail.3783152360)

(defun main (&rest argv)
  (declare (ignorable argv))
  (lisnail:repl)
)
;;; vim: set ft=lisp lisp:
